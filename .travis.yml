language: node_js
node_js:
  - 8.11.1

branches:
  only:
    - master

before_script:
  - npm install -g ionic cordova

script:
  - npm i
  - rm -rf platforms
  - ionic cordova platform add android
  - ionic cordova build